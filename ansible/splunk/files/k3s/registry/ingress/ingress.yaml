---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: registry
  namespace: registry
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`cr.lab.local`)
      services:
        - kind: Service
          name: registry
          port: 5000
