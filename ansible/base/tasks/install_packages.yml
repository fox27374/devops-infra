---
- name: Update/Upgrade apt archives
  ansible.builtin.apt:
    update_cache: yes
    upgrade: "yes"
    cache_valid_time: 86400
  become: yes

- name: Install packages on bastion host
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
  loop: "{{ packages }}" 
  become: yes