apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: guacamole-ingress
  namespace: guacamole
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: PathPrefix(`/`)
      services:
        - kind: Service
          name: guacamole
          port: 80
